<fieldset>
  <% User.where(id: builder.object.user_id).each do |u| %>
    <li>
      <%= u.name %>
      <%= builder.hidden_field :_destroy %>
      <%= link_to "remove", '#', class: "remove_fields" %>
    </li>
    <br/>
  <% end %>
</fieldset>
