<fieldset>
  <%= builder.label "Add a Collaborator" %><br/>
  <%= builder.collection_select :user_id, User.exclude_list(current_user, @wiki), :id, :name %>
  <%= builder.hidden_field :_destroy %>
  <%= link_to "remove", '#', class: "remove_fields" %>
  <hr/>
</fieldset>
